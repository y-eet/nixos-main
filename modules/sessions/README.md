# Sessions

Put display-server / compositor sessions here.

Current:
- `hyprland.nix`

Good future additions:
- `niri.nix`
- `mango.nix` / custom session module, if you later build one
- `plasma.nix`, if you want a temporary fallback desktop

A host chooses sessions by importing them from `hosts/<host>/configuration.nix`.
